{{- if index .Values "platform-ai" "enabled" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-ai
  labels:
    {{- include "platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: ai
spec:
  replicas: {{ index .Values "platform-ai" "replicaCount" }}
  selector:
    matchLabels:
      {{- include "platform.selectorLabels" (dict "component" "platform-ai" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "platform.selectorLabels" (dict "component" "platform-ai" "root" .) | nindent 8 }}
    spec:
      containers:
        - name: platform-ai
          image: "{{ .Values.global.image.registry }}{{ index .Values "platform-ai" "image" "repository" }}:{{ .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ index .Values "platform-ai" "service" "port" }}
          env:
            - name: DB_URL
              value: {{ include "platform.databaseUrl" . | quote }}
            - name: DB_USERNAME
              value: {{ .Values.postgresql.auth.username | quote }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-postgresql
                  key: password
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ include "platform.kafkaBootstrapServers" . | quote }}
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ index .Values "platform-ai" "existingSecret" }}
                  key: ANTHROPIC_API_KEY
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            {{- range $k, $v := (index .Values "platform-ai" "env") }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          resources:
            {{- toYaml (index .Values "platform-ai" "resources") | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ index .Values "platform-ai" "service" "port" }}
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ index .Values "platform-ai" "service" "port" }}
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ai
  labels:
    {{- include "platform.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "platform.selectorLabels" (dict "component" "platform-ai" "root" .) | nindent 4 }}
  ports:
    - port: {{ index .Values "platform-ai" "service" "port" }}
      targetPort: {{ index .Values "platform-ai" "service" "port" }}
{{- end }}
