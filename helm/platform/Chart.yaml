apiVersion: v2
name: platform
description: Test Automation Platform — Helm chart for all platform services
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "16.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
  - name: kafka
    version: "31.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: kafka.enabled
